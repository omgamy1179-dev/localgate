# Coverage config for subprocess measurement (pytest-cov reads
# COVERAGE_PROCESS_START pointing at this file; the end-to-end scenario
# services then report their own coverage into .coverage.* files).
[run]
branch = True
parallel = True
source = localgate
